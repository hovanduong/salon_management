// ignore_for_file: non_constant_identifier_names, constant_identifier_names

import 'package:flutter/material.dart';

const String FONT_FAMILY_NUNITO = 'Quicksand';

const FONT_SIZE_VERY_SMALL = 10.0;
const FONT_SIZE_SMALL = 13.0;
const FONT_SIZE_MEDIUM = 16.0;
const FONT_SIZE_LARGE = 18.0;
const FONT_SIZE_BIG = 22.0;

const FONT_SIZE_VERY_BIG = 30.0; // NOTE: FOR SERVICE LIST (FIRST LETTERS)

const TEXT_HEIGHT = 1.2;

const STYLE_VERY_SMALL = TextStyle(
  fontSize: FONT_SIZE_SMALL,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
);

const STYLE_SMALL = TextStyle(
  fontSize: FONT_SIZE_SMALL,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
);

const STYLE_MEDIUM = TextStyle(
  fontSize: FONT_SIZE_MEDIUM,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
);

const STYLE_LARGE = TextStyle(
  fontSize: FONT_SIZE_LARGE,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
);

const STYLE_BIG = TextStyle(
  fontSize: FONT_SIZE_BIG,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
);

const STYLE_VERY_SMALL_BOLE = TextStyle(
  fontSize: FONT_SIZE_SMALL,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
  fontWeight: FontWeight.bold,
);

const STYLE_SMALL_BOLD = TextStyle(
  fontSize: FONT_SIZE_SMALL,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
  fontWeight: FontWeight.bold,
);

const STYLE_MEDIUM_BOLD = TextStyle(
  fontSize: FONT_SIZE_MEDIUM,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
  fontWeight: FontWeight.bold,
);

const STYLE_LARGE_BOLD = TextStyle(
  fontSize: FONT_SIZE_LARGE,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
  fontWeight: FontWeight.bold,
);

const STYLE_LARGE_BIG = TextStyle(
  fontSize: FONT_SIZE_BIG,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
  fontWeight: FontWeight.bold,
);


// NOTE: FOR SERVICE LIST (FIRST LETTERS)
const STYLE_VERY_BIG = TextStyle(
  fontSize: FONT_SIZE_VERY_BIG,
  height: TEXT_HEIGHT,
  fontFamily: FONT_FAMILY_NUNITO,
  fontWeight: FontWeight.bold,
);
